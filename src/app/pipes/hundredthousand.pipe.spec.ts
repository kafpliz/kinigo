import { HundredthousandPipe } from './hundredthousand.pipe';

describe('HundredthousandPipe', () => {
  it('create an instance', () => {
    const pipe = new HundredthousandPipe();
    expect(pipe).toBeTruthy();
  });
});
